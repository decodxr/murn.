[package]
name = "murn-desktop"
version = "0.14.0"
description = "Native desktop shell for murn. with full and mini voice modes."
authors = ["murn."]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
