[package]
name = "murn-desktop"
version = "0.1.0"
description = "Native desktop shell for murn."
authors = ["murn."]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
